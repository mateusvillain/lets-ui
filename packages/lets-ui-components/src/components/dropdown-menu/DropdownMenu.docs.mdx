import { Meta, Canvas, Controls, ArgTypes } from '@storybook/addon-docs/blocks';
import * as DropdownMenuStories from './DropdownMenu.stories';

<Meta of={DropdownMenuStories} />

# Dropdown Menu

Menu contextual flutuante que agrupa ações relacionadas a um elemento ou contexto. O trigger é livre — qualquer botão, icon-button ou link pode abrir o menu, sem acoplamento ao componente.

<Canvas of={DropdownMenuStories.Default} />
<Controls of={DropdownMenuStories.Default} />

## Shortcut

Use quando as ações possuem atalhos de teclado conhecidos pelos usuários. O shortcut é renderizado com as teclas alinhadas à direita do item.

<Canvas of={DropdownMenuStories.WithShortcuts} />

## Submenu

Use para agrupar ações relacionadas em um segundo nível de navegação. O submenu abre ao passar o mouse ou focar no item pai — evite mais de um nível de aninhamento.

<Canvas of={DropdownMenuStories.WithSubmenu} />

## Divisor

Use divisores para separar visualmente grupos de ações com propósitos distintos dentro do mesmo menu.

<Canvas of={DropdownMenuStories.WithDivider} />

## Item desabilitado

Use para indicar ações temporariamente indisponíveis sem remover a opção da interface.

<Canvas of={DropdownMenuStories.WithDisabledItem} />

## Classe CSS (sem Web Component)

<Canvas of={DropdownMenuStories.CSSClass} />

## Uso

### Web Component

```html
<lui-dropdown-menu>
  <lui-button slot="trigger" variant="secondary" size="md" label="Opções"></lui-button>
  <lui-menu-item slot="items" label="Editar" shortcut="⌘,E"></lui-menu-item>
  <lui-menu-divider slot="items"></lui-menu-divider>
  <lui-menu-item slot="items" label="Excluir" variant="danger"></lui-menu-item>
</lui-dropdown-menu>
```

### Classe CSS

```html
<div class="dropdown-menu">
  <button class="btn btn--secondary btn--md" aria-expanded="false">Opções</button>
  <div class="dropdown-menu__panel is-open">
    <button class="menu-item"><span class="menu-item__label">Editar</span></button>
    <button class="menu-item menu-item--danger"><span class="menu-item__label">Excluir</span></button>
  </div>
</div>
```
