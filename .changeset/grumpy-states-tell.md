---
'@lets-ui/components': minor
'@lets-ui/styles': minor
'@lets-ui/tokens': minor
---

- New layout primitive components: `lui-box`, `lui-stack`, `lui-inline`, `lui-center`, `lui-sidebar`, `lui-switcher`, `lui-columns` (+ `lui-column`), `lui-flex` (+ `lui-flex-item`), `lui-grid` (+ `lui-grid-item`), `lui-container`, `lui-float`, and `lui-scroll-area`.
- CSS-only implementations for `lui-box`, `lui-stack`, `lui-inline`, `lui-center`, `lui-sidebar`, `lui-switcher`, `lui-columns`, `lui-flex`, `lui-grid`, and `lui-container` added to `packages/styles/src/` and compiled into `letsui.min.css`; `lui-float` and `lui-scroll-area` are JS-driven and intentionally excluded from the global stylesheet.
